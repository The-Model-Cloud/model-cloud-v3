export const doesModelMatchJob=(model,job)=>{if(!model||!job)return!1;const genderMatch=job.gender?.length>0&&model.gender?.length>0&&job.gender.every((g=>model.gender.includes(g))),categoryMatch=job.categories?.some((cat=>model.categories?.includes(cat)));return genderMatch&&categoryMatch};