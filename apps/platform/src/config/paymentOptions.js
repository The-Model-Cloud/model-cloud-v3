// Centralized payment options used across the application
// Add new currencies or rate types here to make them available everywhere

export const currencies = ["GBP", "EUR", "USD"];

export const rateTypes = ["Flat Fee", "Hourly", "Per Day"];

export default {
  currencies,
  rateTypes,
};
